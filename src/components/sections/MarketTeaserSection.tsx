"use client";

/**
 * Market Teaser Transition is unified directly inside VerificationSection
 * (matching the unified HeroProblemTransition architecture) to guarantee 100%
 * zero-gap, zero-glitch, smooth mobile and desktop scrolling into WhyLinksuppliedSection.
 */
export function MarketTeaserSection() {
  return null;
}

